---
description: Check docs vs code drift after a change
---
Compare the implementation with docs/03_DATA_MODEL.md, docs/04_API_SPEC.md, docs/05_SMART_CONTRACT.md and
frontend `src/api/types.ts`. List every mismatch (field names, enums, routes, status codes, error codes).
Propose which side to change. Spec changes to 02_ALGORITHMS.md require an ADR — never edit it silently.
Apply only after I confirm.
